language: python
python:
  - "3.6"
sudo: required

jdk:
  - oraclejdk8

sudo: required
install:  
  - sh ci/install_requirements.sh
  - pip install coveralls
  - pip install codecov

script:
     - gradle --version
     - gradle check
     - gradle project-cobol-unit-test:testUnit project-cobol-unit-test:computeTestCoverage
     - cp project-cobol-unit-test/build/CobolUnit/cov.xml coverage.xml

after_success:
  - coveralls
  - codecov
